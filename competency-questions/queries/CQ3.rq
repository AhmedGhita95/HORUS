PREFIX h: <http://www.semanticweb.org/ahmedghita/ontologies/2025/HORUS/v6#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT DISTINCT ?action ?place WHERE {
    ?event a h:Action ; rdfs:label ?action ; h:occursIn ?location .
    ?location rdfs:label ?place .
}
ORDER BY ?action ?place
